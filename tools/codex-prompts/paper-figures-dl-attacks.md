## Sections to embed paper figures in

- `f:/coding/SJ Wiki/docs/cs/deep-learning/`
- `f:/coding/SJ Wiki/docs/cs/adversarial-attacks/`
- `f:/coding/SJ Wiki/docs/cs/reinforcement-learning/`
- `f:/coding/SJ Wiki/docs/cs/machine-learning/`

Apply the paper figure embed policy above. Prioritize:

1. **Adversarial attacks** — most important per user. Especially `white-box-attacks.md` needs the famous Goodfellow panda figure. Also patches, stop-sign physical attacks, ViT/BERT adversarial examples where relevant.

2. **Deep Learning architectures** — Transformer, ResNet, U-Net, DCGAN, ViT, BERT, LSTM cell, Mamba block. Each architecture page benefits from the canonical paper figure NEXT TO our Mermaid diagram.

3. **RL** — Sutton-Barto agent loop, DQN architecture, AlphaGo MCTS.

4. **ML** — perceptron, decision boundary visualizations from textbooks; SVM margin figure.

### Embed style reminder

For each architecture page, the order should be:
1. Brief intro text.
2. Mermaid detailed architecture (already there) — KEEP IT.
3. Original paper figure embedded right after Mermaid, with caption like:
   `*Figure: Original Transformer architecture from [Vaswani et al., 2017](https://arxiv.org/abs/1706.03762) — embedded under educational fair use with attribution.*`
4. Discussion text continues.

Mermaid provides our annotated, dimensioned version; paper figure provides instant recognition. Both pay off.

### Conservative posture

If you cannot identify a reliable URL for a specific paper figure, **skip** that one and continue with the ones you can. The user accepts that not every paper figure can be embedded.

Begin now.
